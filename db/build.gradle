description = ''
dependencies {
    compile project(':argo-core')
    compile('com.jolbox:bonecp:0.8.0.RELEASE') {
        exclude(module: 'guava')
    }
    compile 'com.jolbox:bonecp-spring:0.8.0.RELEASE'
}
